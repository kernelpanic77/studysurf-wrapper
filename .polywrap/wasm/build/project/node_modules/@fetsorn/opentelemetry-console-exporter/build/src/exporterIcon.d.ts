import { ConsoleExporterOptions } from "./types";
import { SpanExporter, ReadableSpan } from "@opentelemetry/sdk-trace-base";
import { ExportResult } from "@opentelemetry/core";
export declare class ConsoleExporterIcon implements SpanExporter {
    private _min;
    private _max;
    private _isDetailed;
    private _numbers;
    private _spanCounter;
    private _indexMap;
    private _parentMap;
    constructor(config: ConsoleExporterOptions);
    /**
     * Export spans.
     * @param spans
     * @param resultCallback
     */
    export(spans: ReadableSpan[], resultCallback: (result: ExportResult) => void): void;
    /**
     * Shutdown the exporter.
     */
    shutdown(): Promise<void>;
    private _indexSpan;
    private _cacheSpan;
    private _formatIcons;
    private _formatDuration;
    private _formatSpanDetailed;
    private _formatSpan;
    private _sendSpans;
}
//# sourceMappingURL=exporterIcon.d.ts.map