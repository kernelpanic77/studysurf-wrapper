import { Uri, UriRedirect, InterfaceImplementations, WrapError } from "../types";
import { Result } from "@polywrap/result";
export declare const getImplementations: (wrapperInterfaceUri: Uri, interfaces: readonly InterfaceImplementations<Uri>[], redirects?: readonly UriRedirect<Uri>[] | undefined) => Result<Uri[], WrapError>;
