import { IWrapPackage, PluginPackage, Uri, Wrapper, WrapError } from "../types";
import { WrapManifest } from "@polywrap/wrap-manifest-types-js";
import { Result } from "@polywrap/result";
export declare class PluginWrapPackage implements IWrapPackage {
    uri: Uri;
    private readonly pluginPackage;
    constructor(uri: Uri, pluginPackage: PluginPackage<unknown>);
    getManifest(): Promise<Result<WrapManifest, WrapError>>;
    createWrapper(): Promise<Result<Wrapper, never>>;
}
