import { Wrapper, Client, InvokeOptions, InvocableResult, PluginPackage, Uri, GetFileOptions, GetManifestOptions } from "../.";
import { WrapManifest } from "@polywrap/wrap-manifest-types-js";
import { Result } from "@polywrap/result";
export declare class PluginWrapper implements Wrapper {
    private _plugin;
    private _instance;
    constructor(_plugin: PluginPackage<unknown>);
    getFile(_: GetFileOptions): Promise<Result<Uint8Array | string, Error>>;
    getManifest(_?: GetManifestOptions): WrapManifest;
    invoke(options: InvokeOptions<Uri>, client: Client): Promise<InvocableResult<unknown>>;
    private _getInstance;
}
