import { Uri } from ".";
export interface InterfaceImplementations<TUri extends Uri | string = string> {
    interface: TUri;
    implementations: TUri[];
}
export declare const sanitizeInterfaceImplementations: (input: InterfaceImplementations<string | Uri>[]) => InterfaceImplementations<Uri>[];
