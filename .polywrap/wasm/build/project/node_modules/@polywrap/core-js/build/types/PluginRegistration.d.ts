import { PluginPackage, Uri } from ".";
export interface PluginRegistration<TUri extends Uri | string = string> {
    uri: TUri;
    plugin: PluginPackage<unknown>;
}
export declare const sanitizePluginRegistrations: (input: PluginRegistration<string | Uri>[]) => PluginRegistration<Uri>[];
