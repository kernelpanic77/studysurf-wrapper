import { Abi, CapabilityType } from "@polywrap/schema-parse";
export interface SchemaFile {
    schema: string;
    absolutePath: string;
}
export declare type AbiResolver = (uri: string) => Promise<Abi>;
export declare type SchemaResolver = (path: string) => Promise<string>;
export interface AbiResolvers {
    external: AbiResolver;
    local: SchemaResolver;
}
export interface ExternalImport {
    importedTypes: string[];
    namespace: string;
    uri: string;
}
export interface Use {
    usedTypes: CapabilityType[];
    namespace: string;
}
export interface LocalImport {
    importedTypes: string[];
    path: string;
}
export declare const SYNTAX_REFERENCE: string;
