import { Uri, IUriResolver, IWrapPackage, UriPackageOrWrapper } from "@polywrap/core-js";
import { Result } from "@polywrap/result";
export declare class PackageResolver implements IUriResolver {
    private uri;
    private wrapPackage;
    constructor(uri: Uri, wrapPackage: IWrapPackage);
    get name(): string;
    tryResolveUri(uri: Uri): Promise<Result<UriPackageOrWrapper>>;
}
