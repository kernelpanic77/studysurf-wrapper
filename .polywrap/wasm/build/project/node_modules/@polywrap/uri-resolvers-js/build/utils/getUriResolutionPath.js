"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getUriResolutionPath = void 0;
var getUriResolutionPath = function (history) {
    // Get all non-empty items from the resolution history
    return (history
        .filter(function (x) {
        // If a failure happened
        if (!x.result.ok) {
            return true;
        }
        var uriPackageOrWrapper = x.result.value;
        if (uriPackageOrWrapper.type === "uri") {
            // If a redirect has occured
            return uriPackageOrWrapper.uri.uri !== x.sourceUri.uri;
        }
        else if (uriPackageOrWrapper.type === "package") {
            // If a package was returned
            return true;
        }
        else if (uriPackageOrWrapper.type === "wrapper") {
            // If a wrapper was returned
            return true;
        }
        return false;
    })
        // Filter all sub-history
        .map(function (x) {
        if (x.subHistory && x.subHistory.length) {
            return __assign(__assign({}, x), { subHistory: exports.getUriResolutionPath(x.subHistory) });
        }
        else {
            return x;
        }
    }));
};
exports.getUriResolutionPath = getUriResolutionPath;
//# sourceMappingURL=getUriResolutionPath.js.map