"use strict";
/* eslint-disable no-empty */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.migrateAnyManifest = void 0;
var yaml_1 = __importDefault(require("yaml"));
function migrateAnyManifest(manifestString, manifestTypeName, migrateFn, to, logger) {
    var manifest;
    try {
        manifest = JSON.parse(manifestString);
    }
    catch (_) {
        try {
            manifest = yaml_1.default.parse(manifestString);
        }
        catch (_) { }
    }
    if (!manifest) {
        throw Error("Unable to parse " + manifestTypeName + ": " + manifestString);
    }
    var newManifest = migrateFn(manifest, to, logger);
    var cleanedManifest = JSON.parse(JSON.stringify(newManifest));
    delete cleanedManifest.__type;
    return yaml_1.default.stringify(cleanedManifest, null, 2);
}
exports.migrateAnyManifest = migrateAnyManifest;
//# sourceMappingURL=migrateAnyManifest.js.map