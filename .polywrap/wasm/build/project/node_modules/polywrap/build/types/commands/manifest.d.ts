import { Command } from "./types";
declare const manifestTypes: readonly ["project", "build", "deploy", "infra", "meta", "workflow"];
declare type ManifestType = typeof manifestTypes[number];
declare type ManifestSchemaCommandOptions = {
    raw: boolean;
    manifestFile: ManifestType;
    verbose?: boolean;
    quiet?: boolean;
};
export declare const manifest: Command;
export declare const runSchemaCommand: (type: ManifestType, options: ManifestSchemaCommandOptions) => Promise<void>;
export {};
