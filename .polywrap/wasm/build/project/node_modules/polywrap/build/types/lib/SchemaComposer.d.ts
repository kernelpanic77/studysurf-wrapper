import { Project, AnyProjectManifest } from "./";
import { PolywrapClient } from "@polywrap/client-js";
import { WrapAbi } from "@polywrap/schema-parse";
export interface SchemaComposerConfig {
    project: Project<AnyProjectManifest>;
    client: PolywrapClient;
}
export declare class SchemaComposer {
    private _config;
    private _client;
    private _abi;
    constructor(_config: SchemaComposerConfig);
    getComposedAbis(): Promise<WrapAbi>;
    reset(): void;
    private _fetchLocalSchema;
    private _fetchExternalAbi;
    private _loadGraphqlAbi;
    private _loadWrapAbi;
    private _loadJsonAbi;
    private _loadYamlAbi;
}
