import { BuildStrategyArgs, BuildStrategy } from "../BuildStrategy";
export interface VMConfig {
    defaultIncludes: string[];
    baseImage: string;
    version: string;
}
export declare class DockerVMBuildStrategy extends BuildStrategy<void> {
    private _volumePaths;
    constructor(args: BuildStrategyArgs);
    getStrategyName(): string;
    buildSources(): Promise<void>;
    private _buildSources;
    private _copyBuildOutput;
}
