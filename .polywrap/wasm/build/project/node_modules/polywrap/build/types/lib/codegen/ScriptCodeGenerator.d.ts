import { AnyProjectManifest, Project } from "../project";
import { SchemaComposer } from "../SchemaComposer";
import { CodeGenerator } from "./CodeGenerator";
import { BindLanguage } from "@polywrap/schema-bind";
export declare class ScriptCodegenerator extends CodeGenerator {
    private readonly _script;
    private readonly _mustacheView;
    private readonly _codegenDirAbs;
    private readonly _omitHeader;
    private readonly _schema;
    constructor(config: {
        project: Project<AnyProjectManifest>;
        schemaComposer: SchemaComposer;
        codegenDirAbs: string;
        script: string;
        mustacheView: Record<string, unknown> | undefined;
        omitHeader: boolean;
        schema?: string;
    });
    protected runCodegen(bindLanguage: BindLanguage): Promise<string[]>;
    private _generateTemplate;
}
