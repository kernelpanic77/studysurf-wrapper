import { Deployer } from "../../../deploy";
import { Uri } from "@polywrap/core-js";
declare class ENSRecursiveNameRegisterPublisher implements Deployer {
    execute(uri: Uri, config: {
        provider: string;
        privateKey?: string;
        ensRegistryAddress: string;
        ensRegistrarAddress: string;
        ensResolverAddress: string;
    }): Promise<Uri>;
}
declare const _default: ENSRecursiveNameRegisterPublisher;
export default _default;
