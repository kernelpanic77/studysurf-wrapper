import { Deployer } from "../../../deploy/deployer";
import { Uri } from "@polywrap/core-js";
declare class HTTPDeployer implements Deployer {
    execute(uri: Uri, config?: {
        postUrl: string;
    }): Promise<Uri>;
}
declare const _default: HTTPDeployer;
export default _default;
