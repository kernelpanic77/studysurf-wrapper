import { DeployStep } from "./step";
import { Logger } from "../logging";
import { Uri } from "@polywrap/core-js";
export interface DeployJobResult {
    name: string;
    steps: {
        id: string;
        name: string;
        input: Uri;
        result: Uri;
    }[];
}
interface DeployJobArgs {
    name: string;
    steps: DeployStep[];
    config: Record<string, unknown>;
    logger: Logger;
}
export declare class DeployJob {
    name: string;
    steps: DeployStep[];
    config: Record<string, unknown>;
    private _logger;
    private _resultMap;
    constructor(config: DeployJobArgs);
    run(): Promise<DeployJobResult>;
    private _getUriArgument;
}
export {};
