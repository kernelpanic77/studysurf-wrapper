import { Deployer } from "./deployer";
import { Uri } from "@polywrap/core-js";
export declare type StepName = string;
export declare type UriOrPrevStepResult = string;
export interface StepResult {
    id: string;
    input: Uri;
    result: Uri;
}
interface StepArgs {
    name: string;
    uriOrStepResult: UriOrPrevStepResult;
    deployer: Deployer;
    config: Record<string, unknown>;
}
export declare class DeployStep {
    name: string;
    deployer: Deployer;
    uriOrStepResult: string;
    config: Record<string, unknown>;
    constructor(args: StepArgs);
    run(uri: Uri, config: Record<string, unknown>): Promise<Uri>;
}
export {};
