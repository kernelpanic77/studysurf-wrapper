import { Logger } from "../logging";
import { MetaManifest } from "@polywrap/polywrap-manifest-types-js";
export declare function outputMetadata(metaManifest: MetaManifest, outputDir: string, rootDir: string, logger: Logger): Promise<MetaManifest>;
