import { Logger } from "../logging";
import { WrapAbi } from "@polywrap/wrap-manifest-types-js";
export declare const generateWrapFile: (abi: WrapAbi, name: string, type: "interface" | "wasm" | "plugin", path: string, logger: Logger) => Promise<void>;
