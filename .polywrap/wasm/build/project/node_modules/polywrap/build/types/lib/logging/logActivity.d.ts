import { Logger } from "./Logger";
export declare function logActivity<TReturn>(logger: Logger, infoText: string, errorText: string, warningText: string, execute: (logger: Logger) => Promise<TReturn>): Promise<TReturn>;
