import { Log } from "../Log";
import { LogLevel } from "@polywrap/logging-js";
export declare class FileLog extends Log {
    readonly logFilePath: string;
    private _logFileStream;
    constructor(logFilePath: string, level: LogLevel);
    end(): void;
    log(message: string, level: LogLevel): void;
    private _createWriteStream;
}
