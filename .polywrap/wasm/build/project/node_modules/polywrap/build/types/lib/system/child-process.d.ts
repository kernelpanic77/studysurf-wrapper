/// <reference types="node" />
import { Logger } from "../logging";
import { SpawnSyncReturns } from "child_process";
export declare function runCommandSync(command: string, logger: Logger, env?: Record<string, string> | undefined): {
    stdout?: string;
    stderr?: SpawnSyncReturns<string> & Error;
};
export declare function runCommand(command: string, logger: Logger, env?: Record<string, string> | undefined, cwd?: string | undefined, redirectStderr?: boolean): Promise<{
    stdout: string;
    stderr: string;
}>;
